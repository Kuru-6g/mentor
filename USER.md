# USER.md

The user is a developer working on the `mentor` project.

- **Stack**: React, TypeScript, Vite, Supabase.
- **Preferences**:
  - Prefers functional components.
  - Uses Tailwind CSS (if applicable, though I saw Vanilla CSS was preferred in the metadata).
  - Uses Supabase for backend services.
